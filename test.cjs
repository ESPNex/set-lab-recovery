const {chromium}=require('playwright');(async()=>{const b=await chromium.launch({headless:true,args:['--no-sandbox']});const page=await b.newPage({viewport:{width:1600,height:1100}});let errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto('file:///home/user/viewer.html');await page.waitForTimeout(700);console.log('images',await page.locator('svg image').count());await page.screenshot({path:'analysis/viewer-check.png',fullPage:true});await page.locator('.pair').nth(1).click();console.log('pair2',await page.locator('svg image').count());await page.locator('.pair').nth(2).click();console.log('pair3',await page.locator('svg image').count());await page.locator('#bare').click();console.log('bare',await page.locator('svg image').count());await page.locator('#all').click();await page.locator('#zoomIn').click();console.log('errors',errors);await b.close()})()
